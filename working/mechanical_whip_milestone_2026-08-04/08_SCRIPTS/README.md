# Preserved Scripts

This folder contains only source code that was actually present among the uploaded files.

Included:

- `test_support_tangent_cycle_audit.py`

The rotating-memory and single-whip reports, logs, raw arrays, tables, and plots are preserved, but their source scripts were not present in the active uploaded-file set. Their exact recorded commands and parameters are listed in `00_README/REPRODUCIBILITY.md`.
